namespace GTA.UI
{
    public enum MiniMapComponent
    {
        minimap,
        minimap_mask,
        minimap_blur,
        bigmap,
        bigmap_mask,
        bigmap_blur,
        pausemap,
        pausemap_mask,
        gallery,
        gallery_mask,
        golf_courses,
    }
}
